<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leyou.dao.TbBrandMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.leyou.entity.TbBrand">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="image" property="image" />
        <result column="letter" property="letter" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, image, letter
    </sql>

    <select id="queryByPageandRows" parameterType="int" resultType="com.leyou.entity.TbBrand">
        select * from tb_brand limit #{page},#{rows};
    </select>

    <select id="queryByPageandRowsandKey" parameterType="int" resultType="com.leyou.entity.TbBrand">
        select * from tb_brand limit #{page},#{rows};
    </select>

</mapper>
